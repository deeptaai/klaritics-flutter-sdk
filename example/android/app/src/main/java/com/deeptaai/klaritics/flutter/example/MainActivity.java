package com.deeptaai.klaritics.flutter.example;

import io.flutter.embedding.android.FlutterActivity;

public class MainActivity extends FlutterActivity {
    // Klaritics is initialized from Dart via KlariticsFlutter.setup(config) in main().
}
